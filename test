 const changeTableData = (id) => {
    setTableData((current) => {
      const newState = [...current];
      const index = newState.findIndex((row) => row.nReportCondId === id);

      if (index !== -1) {
        newState[index].checked = !newState[index].checked;
      }

      return newState;
    });
  };
